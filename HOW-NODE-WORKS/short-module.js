// exports.add = (a, b) => {
//   return a + b;
// };

// exports.sub = (a, b) => {
//   return a - b;
// }; // legacy code commonJS

export const add = (a, b) => a + b;
export const sub = (a, b) => a - b; // ES6 version new code
